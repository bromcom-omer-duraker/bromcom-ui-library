import { Component, Prop, h } from '@stencil/core';

@Component({
    tag: 'bcm-comment',
    styleUrl: 'comment.scss',
    shadow: true
})
export class BcmComment {
    render() {
        return ('')
    }
}
